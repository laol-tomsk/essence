module AlphasHelper
  def get_grouped_subalphas_tree(alpha)
    subalphaTypes = alpha.definition.subalpha_definitions
    subalphas = alpha.subalphas
    result = []

    subalphas.each do |subalpha|
      result.push({

                  })
    end
    el_model = {
        type: subalphas
    }
  end
end
